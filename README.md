# cloud-data-quality-project_sample
This project demonstrates an automated data pipeline that ingests CSV files from Azure Blob Storage into Snowflake, applies transformations, and validates data quality.
